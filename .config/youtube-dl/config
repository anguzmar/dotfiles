# Save in ~/Music
-o ~/Media/Music/Main/%(title)s.%(ext)s

# Ignore errors when downloading a playlist.
-i

# Audio quality (between 0 and 9, default is 5).
--audio-quality 5

# Keep only audio files.
-f bestaudio

# Audio format.
--audio-format "mp3"
